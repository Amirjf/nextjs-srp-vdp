import styled from 'styled-components';

export const FiltersContainer = styled.div({
  display: 'flex',
  justifyContent: 'center',
  gap: 10,
});

export const FiltersOptionsWrapper = styled.div({
  backgroundColor: '#fff',
  width: '100%',
  height: '100vh',
});
